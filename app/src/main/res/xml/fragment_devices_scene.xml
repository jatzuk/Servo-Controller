<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto">

    <Transition
        motion:constraintSetEnd="@id/end"
        motion:constraintSetStart="@id/start"
        motion:duration="1500"
        motion:motionInterpolator="easeOut"></Transition>

    <ConstraintSet android:id="@+id/start">
        <Constraint android:id="@+id/card_view_available_devices">
            <PropertySet />
        </Constraint>
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end">
        <Constraint android:id="@+id/card_view_available_devices">
            <Layout
                motion:layout_constraintBottom_toBottomOf="@+id/card_view_paired_devices"
                motion:layout_constraintEnd_toEndOf="parent"
                motion:layout_constraintStart_toStartOf="parent"
                motion:layout_constraintTop_toTopOf="parent" />
            <PropertySet android:layout_margin="8dp" />
        </Constraint>
    </ConstraintSet>

</MotionScene>
